name: Deploy via FTP to Hostinger

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy to Hostinger via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 82.197.89.47
          username: 'u906310247.Testing123'
          password: '/Nz!JdGZufu[dV3b'
          server-dir: '/'
          protocol: ftp
          port: 21
          timeout: 120000
          passive: false
          exclude: |
            **/.git*
            **/.github/**
            **/node_modules/**
            **/vendor/**
            **/.env*
            **/storage/logs/**
            **/bootstrap/cache/**
