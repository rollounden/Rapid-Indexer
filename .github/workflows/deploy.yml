name: Deploy via FTPS to Hostinger

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy to Hostinger via FTPS
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ftp.cyan-peafowl-394593.hostingersite.com
          username: 'u906310247.Testing123'
          password: '/Nz!JdGZufu[dV3b'
          server-dir: '/'
          protocol: ftps
          port: 21
          exclude: |
            **/.git*
            **/.github/**
            **/node_modules/**
            **/vendor/**
            **/.env*
            **/storage/logs/**
            **/bootstrap/cache/**
