name: Deploy via FTP to Hostinger

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy to Hostinger via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 82.197.89.47
          username: 'u906310247.Testing123'
          password: '/Nz!JdGZufu[dV3b'
          server-dir: '/'
          protocol: ftp
          port: 21
          timeout: 120000
          dangerous-clean-slate: true
          exclude: |
            **/.git*
            **/.github/**
            **/.cursor/**
            **/docs/**
            **/node_modules/**
            **/vendor/**
            **/.env*
            **/storage/logs/**
            **/bootstrap/cache/**
            **/AUTO_SYNC_SETUP.md
            **/cron_test.php
            **/debug_api_response.php
            **/manual_task_sync.php
            **/test_fixed_sync.php
            **/analyze_api_response.php
